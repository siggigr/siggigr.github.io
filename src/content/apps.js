/**
 * Apps shown in the Apps section. One card per app:
 *   { name, description, url }
 * Leave `url` as an empty string to show a "coming soon" tag.
 * While this list is empty, the section shows the "In the hangar" card.
 */
export const apps = [
  // { name: "DayPlan", description: "Shared day itineraries.", url: "" },
];
